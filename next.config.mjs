/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['fakestoreapi.com'], // Allow this domain for external images
    },
  };
  
  export default nextConfig;
  